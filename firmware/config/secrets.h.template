/*
 * secrets.h — WiFi & Agent Credentials
 *
 * SETUP:
 *   cp secrets.h.template secrets.h
 *   Edit secrets.h with your WiFi SSID, password, and laptop IP.
 *
 * This file is .gitignore'd. NEVER commit real credentials.
 */

#ifndef SECRETS_H
#define SECRETS_H

#define WIFI_SSID     "YOUR_WIFI_SSID"
#define WIFI_PASS     "YOUR_WIFI_PASSWORD"
#define AGENT_IP      192, 168, 1, 100   // Your laptop's IP (comma-separated)
#define AGENT_PORT    8888               // Must match: micro-ros-agent --port 8888

#endif // SECRETS_H
