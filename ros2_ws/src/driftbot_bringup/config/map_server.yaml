# Map server config for Nav2 (uses SLAM map at runtime, this is for initial pose)
# Note: Nav2 will use the SLAM-provided /map topic; this file is a placeholder for lifecycle
# The actual map is provided by slam_toolbox at runtime

map_server:
  ros__parameters:
    use_sim_time: True
    yaml_filename: ""
    topic_name: "map"
    frame_id: "map"